# Web framework & ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Async RabbitMQ client
pika>=1.3.2

# Data validation (FastAPI pulls it automatically, but explicit version avoids conflicts)
pydantic>=2.0.0